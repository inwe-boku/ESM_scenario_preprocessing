# CLAUDE.md

This repository contains tooling for preprocessing scenario input data.

## Development

- Use `pixi` to manage environments and dependencies.
- The Python package uses a `src/` layout.
- Put local input data in `resources/` (not version controlled).
